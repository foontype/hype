
templates:
  hype:
    - name: {{ .Environments.name }}-configmap
      type: state-value-file
      values:
        hoge: 12345

    - name: {{ .Environment.name }}-secrets
      type: secrets-default
      values:
        fuga: 23456

releases:
  ...